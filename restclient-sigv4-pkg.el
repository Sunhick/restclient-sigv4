;;; restclient-sigv4-pkg.el --- Package metadata for restclient-sigv4  -*- no-byte-compile: t; -*-

;; This file is not part of GNU Emacs.

;;; Commentary:

;; This file is used by the Emacs package system when installing from
;; a multi-file package archive.  MELPA generates this automatically
;; from the headers in restclient-sigv4.el.

;;; Code:

(define-package "restclient-sigv4" "0.1.0"
  "AWS SigV4 signing for restclient.el"
  '((emacs "27.1")
    (restclient "0"))
  :keywords '("comm" "tools")
  :url "https://github.com/Sunhick/restclient-sigv4")

;;; restclient-sigv4-pkg.el ends here
